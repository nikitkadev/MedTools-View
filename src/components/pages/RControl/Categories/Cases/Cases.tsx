import styles from './styles.module.scss';

const Cases = () => {
    return (
        <section className={styles.casesRoot}>
            Пук с падливкой для случаев
        </section>
    )
};

export default Cases;